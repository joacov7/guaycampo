apiVersion: v1
kind: Service
metadata:
  name: guaycampo-web
  labels:
    app: guaycampo-web
    app.kubernetes.io/name: guaycampo-web
    app.kubernetes.io/component: frontend
spec:
  selector:
    app: guaycampo-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
