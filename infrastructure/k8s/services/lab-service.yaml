apiVersion: v1
kind: Service
metadata:
  name: guaycampo-lab
  labels:
    app: guaycampo-lab
    app.kubernetes.io/name: guaycampo-lab
    app.kubernetes.io/component: service
spec:
  selector:
    app: guaycampo-lab
  ports:
    - name: http
      port: 3004
      targetPort: 3004
      protocol: TCP
  type: ClusterIP
