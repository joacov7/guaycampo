apiVersion: apps/v1
kind: Deployment
metadata:
  name: guaycampo-shifts
  labels:
    app: guaycampo-shifts
    app.kubernetes.io/name: guaycampo-shifts
    app.kubernetes.io/component: service
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: guaycampo-shifts
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: guaycampo-shifts
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: shifts
          image: guaycampo-shifts:latest
          ports:
            - containerPort: 3002
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: NODE_ENV
            - name: PORT
              value: "3002"
            - name: SHIFTS_SERVICE_PORT
              value: "3002"
            - name: TZ
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: TZ
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: LOG_LEVEL
            - name: JWT_EXPIRES_IN
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: JWT_EXPIRES_IN
            - name: JWT_REFRESH_EXPIRES_IN
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: JWT_REFRESH_EXPIRES_IN
            - name: AUTH_SERVICE_HOST
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: AUTH_SERVICE_HOST
            - name: AUTH_SERVICE_PORT
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: AUTH_SERVICE_PORT
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: guaycampo-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: guaycampo-secrets
                  key: redis-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: guaycampo-secrets
                  key: jwt-secret
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values: [guaycampo-shifts]
                topologyKey: kubernetes.io/hostname
