apiVersion: apps/v1
kind: Deployment
metadata:
  name: guaycampo-auth
  labels:
    app: guaycampo-auth
    app.kubernetes.io/name: guaycampo-auth
    app.kubernetes.io/component: service
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: guaycampo-auth
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: guaycampo-auth
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: auth
          image: guaycampo-auth:latest
          ports:
            - containerPort: 3001
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: NODE_ENV
            - name: PORT
              value: "3001"
            - name: AUTH_SERVICE_PORT
              value: "3001"
            - name: TZ
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: TZ
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: LOG_LEVEL
            - name: JWT_EXPIRES_IN
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: JWT_EXPIRES_IN
            - name: JWT_REFRESH_EXPIRES_IN
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: JWT_REFRESH_EXPIRES_IN
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: guaycampo-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: guaycampo-secrets
                  key: redis-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: guaycampo-secrets
                  key: jwt-secret
            - name: JWT_REFRESH_SECRET
              valueFrom:
                secretKeyRef:
                  name: guaycampo-secrets
                  key: jwt-refresh-secret
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values: [guaycampo-auth]
                topologyKey: kubernetes.io/hostname
