apiVersion: apps/v1
kind: Deployment
metadata:
  name: guaycampo-lab
  labels:
    app: guaycampo-lab
    app.kubernetes.io/name: guaycampo-lab
    app.kubernetes.io/component: service
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: guaycampo-lab
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: guaycampo-lab
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: lab
          image: guaycampo-lab:latest
          ports:
            - containerPort: 3004
          env:
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: NODE_ENV
            - name: PORT
              value: "3004"
            - name: TZ
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: TZ
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: LOG_LEVEL
            - name: AUTH_SERVICE_HOST
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: AUTH_SERVICE_HOST
            - name: AUTH_SERVICE_PORT
              valueFrom:
                configMapKeyRef:
                  name: guaycampo-common-config
                  key: AUTH_SERVICE_PORT
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: guaycampo-secrets
                  key: database-url
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: guaycampo-secrets
                  key: redis-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: guaycampo-secrets
                  key: jwt-secret
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values: [guaycampo-lab]
                topologyKey: kubernetes.io/hostname
